// TODO: Replace with logger such as winston (Setup was annoying and
// StackdriverTransport was not working with symbols)
const { error, warn, info, log, debug } = console;

export { error, warn, info, log, debug };

export default { error, warn, info, log, debug };
